import React from "react";
import PhotosWrapper from "../components/photo/PhotosWrapper";

const HomePage: React.FC = () => {
  return (
    <div>
      <PhotosWrapper />
    </div>
  );
};

export default HomePage;
